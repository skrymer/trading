---
description: Run all pre-commit checks (backend tests, ktlint, detekt, frontend lint, typecheck) and verify CLAUDE.md files
---

Read `.claude/skills/pre-commit.md` and run all 5 code checks in parallel, then verify CLAUDE.md files are up to date. Report results in a summary table.
